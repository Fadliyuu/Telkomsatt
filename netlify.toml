[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[context.production.environment]
  NEXT_PUBLIC_BASE_URL = "https://tsatspare.netlify.app"

# Next.js runtime detected automatically by Netlify.
# Set Firebase and Cloudinary secrets in the Netlify dashboard, not this file.
